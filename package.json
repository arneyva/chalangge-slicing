{
  "devDependencies": {
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "tailwind-scrollbar-hide": "^2.0.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "jalan": "npx tailwindcss -i ./src/input.css -o ./src/output.css --watch"
  }
}
